# ==================== API密钥配置 ====================

# OpenRouter API配置（unnormal模式）
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_MODEL=cognitivecomputations/dolphin-mistral-24b-venice-edition:free

# DeepSeek API配置（normal模式，支持工具调用）
DEEPSEEK_API_KEY=your_deepseek_api_key_here
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# ==================== Tavily搜索API配置 ====================
# 获取API密钥：https://tavily.com/
TAVILY_API_KEY=your_tavily_api_key_here

# ==================== QQ邮箱配置 ====================
# QQ邮箱SMTP服务器配置
QQ_EMAIL_SMTP_SERVER=smtp.qq.com
QQ_EMAIL_SMTP_PORT=587
# 是否使用SSL连接（true使用465端口，false使用587端口+STARTTLS）
QQ_EMAIL_USE_SSL=false
# 发送邮件的QQ邮箱地址
QQ_EMAIL_SENDER=your_qq_email@qq.com
# QQ邮箱授权码（不是QQ密码，需要在QQ邮箱设置中生成）
# 获取方式：QQ邮箱 -> 设置 -> 账户 -> 开启SMTP服务 -> 生成授权码
QQ_EMAIL_PASSWORD=your_qq_email_authorization_code
# 发件人显示名称（可选）
QQ_EMAIL_SENDER_NAME=系统通知
# 连接超时时间（秒，可选，默认30秒）
QQ_EMAIL_TIMEOUT=30

# ==================== Cloudflare Turnstile 配置（可选）====================
# Cloudflare Turnstile 人机验证配置（如果未配置，将跳过人机验证直接验证管理员权限）
# 注意：Turnstile 需要域名，如果只有服务器IP，可以留空不配置
# 获取方式：登录 Cloudflare Dashboard -> Turnstile -> 创建站点
# 参考文档：https://developers.cloudflare.com/turnstile/
# 如果未配置，管理员验证将仅检查用户角色，不进行人机验证
CLOUDFLARE_TURNSTILE_SITE_KEY=
CLOUDFLARE_TURNSTILE_SECRET_KEY=

# ==================== 码支付配置（可选）====================
# 码支付接口配置
# 获取方式：登录 https://pay.mzfpay.com 注册商户并获取商户ID和密钥
# 参考文档：https://pay.mzfpay.com/doc/epay_submit
# 如果未配置，支付功能将无法使用
MZFPAY_PID=your_merchant_id_here
MZFPAY_KEY=your_merchant_key_here
# 支付平台基础URL（可选，默认为 https://pay.mzfpay.com）
MZFPAY_BASE_URL=https://pay.mzfpay.com

# ==================== 应用配置 ====================
DEFAULT_MODE=unnormal
MAX_HISTORY_LENGTH=50
TEMPERATURE=0.7

# ==================== 说明 ====================
# 1. 将本文件复制为 .env
# 2. 填入你的API密钥
# 3. Open-Meteo天气API无需密钥，可直接使用
# 4. Tavily API密钥可在 https://tavily.com/ 获取
# 5. QQ邮箱授权码获取：登录QQ邮箱 -> 设置 -> 账户 -> 开启SMTP服务 -> 生成授权码
# 6. 如果遇到连接问题，可以尝试设置 QQ_EMAIL_USE_SSL=true 并使用465端口
# 7. Cloudflare Turnstile 密钥获取：登录 Cloudflare Dashboard -> Turnstile -> 创建站点

